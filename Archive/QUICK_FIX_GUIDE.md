# Hearthlink Quick Fix Guide\n\n## 🚨 **Current Issue: Black/Purple Screen**\n\nThe black/purple screen indicates that Electron is running but the React app isn't loading properly.\n\n## 🔧 **Immediate Solutions**\n\n### **Step 1: Use Debug Launcher**\n```batch\ndebug-launch.bat\n```\nThis will:\n- Check all required files\n- Show detailed error messages\n- Launch with debug output\n\n### **Step 2: Test Basic Functionality**\nWhen the debug launcher runs, you should see a **test page** with:\n- Hearthlink logo\n- System status checks\n- \"Launch React App\" button\n\nIf you see this test page, the basic system is working.\n\n### **Step 3: Build the React App**\nIf the test page works but React doesn't load:\n```batch\nnpm run build\n```\nThis creates the `build` directory with the React app.\n\n### **Step 4: Use Development Mode**\nFor active development:\n```batch\nlaunch-dev.bat\n```\nThis starts both React dev server and Electron.\n\n## 📋 **Troubleshooting Checklist**\n\n### **✅ Files to Check:**\n- [ ] `package.json` exists\n- [ ] `main.js` exists\n- [ ] `public/index.html` exists\n- [ ] `public/test.html` exists\n- [ ] `public/assets/Hearthlink.png` exists\n- [ ] `src/assets/Hearthlink.png` exists\n- [ ] `node_modules` directory exists\n- [ ] `build` directory exists (for production)\n\n### **✅ What Should Happen:**\n1. **Test page loads** - Shows Hearthlink logo and system status\n2. **Assets load** - Logo appears, no broken images\n3. **React loads** - Main application interface appears\n4. **No console errors** - Check DevTools (F12)\n\n## 🎯 **Expected Behavior by Launch Method**\n\n### **debug-launch.bat**\n- Shows file check results\n- Loads test page first\n- Displays console output\n- Shows any error messages\n\n### **launch-dev.bat**\n- Starts React dev server (http://localhost:3000)\n- Starts Electron pointing to dev server\n- Enables hot reload\n- Shows both console outputs\n\n### **launch-simple.bat**\n- Installs dependencies if needed\n- Starts Electron with built React app\n- Requires `npm run build` first\n\n## 🔍 **Common Issues & Solutions**\n\n### **Issue 1: Test page doesn't load**\n**Cause:** Basic file system issues\n**Solution:**\n```batch\n# Check file structure\ndir public\\test.html\ndir public\\assets\\Hearthlink.png\n\n# If files missing, re-run setup\n```\n\n### **Issue 2: Assets don't load (no logo)**\n**Cause:** Missing Hearthlink.png file\n**Solution:**\n```batch\n# Copy icon file\ncopy \"src\\assets\\Hearthlink.png\" \"public\\assets\\Hearthlink.png\"\n```\n\n### **Issue 3: React app doesn't load**\n**Cause:** Missing build directory\n**Solution:**\n```batch\n# Build React app\nnpm run build\n\n# Then launch\nnpm start\n```\n\n### **Issue 4: Development mode fails**\n**Cause:** Port conflicts or React server issues\n**Solution:**\n```batch\n# Kill any existing processes\ntaskkill /f /im node.exe\ntaskkill /f /im electron.exe\n\n# Start fresh\nlaunch-dev.bat\n```\n\n## 🚀 **Recommended Fix Sequence**\n\n1. **Run debug launcher:**\n   ```batch\n   debug-launch.bat\n   ```\n\n2. **Check what you see:**\n   - ✅ Test page with logo = Basic system OK\n   - ❌ Error page = File system issues\n   - ❌ Blank/black screen = Electron issues\n\n3. **If test page works:**\n   - Click \"Launch React App\" button\n   - If React loads = Success!\n   - If React fails = Need to build\n\n4. **If React needs building:**\n   ```batch\n   npm run build\n   npm start\n   ```\n\n5. **If you want live development:**\n   ```batch\n   launch-dev.bat\n   ```\n\n## 💡 **Pro Tips**\n\n- **Use F12** to open DevTools and check for errors\n- **Check console output** in the command window\n- **Try different launch methods** to isolate the issue\n- **Look for \"Loading URL:\" messages** in console\n- **Verify file permissions** if on network drive\n\n## 🎉 **Success Indicators**\n\nYou'll know it's working when you see:\n- ✅ Hearthlink logo appears\n- ✅ StarCraft-themed interface loads\n- ✅ Radial menu in top-left corner\n- ✅ \"ALDEN CONSTRUCT ONLINE\" message\n- ✅ No console errors\n\n---\n\n**Start with `debug-launch.bat` to diagnose the exact issue!**